


===========revisar CRUD antes do início==================

Ajustar para usar filtro na listagem

	String buscarLivro = request.getParameter("buscarLivro");


	if ((codigoLivro == null || codigoLivro.isEmpty()) && buscarLivro == null) {
		//... adicionar listarTodos dentro de suas estruturas de decisão
		request.setAttribute("livros", dao.listarTodos());


	} else if (buscarLivro != null) {
		List<Livro> livros = dao.consultar(buscarLivro);
		request.setAttribute("livros", livros);


Exercício:

- implementação de Listagem e Inclusão de "Produtos" usando JSP e Servlets
	id; nome; descrição; valor; fornecedor
- criar dynamic web project

- Converter para Maven project (botão direito > Configure > Convert to Maven Project)

- Adicionar dependências de BD e JSTL (jstl 1.2 maven / standard 1.1.2 maven)

- Se necessário: Configurar o servidor (botão direito no projeto > Properties > Project Facets > Runtimes)

- Implementar classe de acesso a dados (DAO e FabricaConexao) 

- Testar conexão (classe TestaConexao)

- Implementar servlet (Camada de controle)

- Implementar JSP's (inclusaoProduto.jsp e ListagemProdutos.jsp)
